<?xml version="1.0" encoding="utf-8"?>
<!--
    🌿 SANA - Manifiesto Principal de Android
    Define permisos, actividades, servicios y configuraciones de la app
    
    NOTAS IMPORTANTES:
    - targetApi="34": Android 14 (Upside Down Cake)
    - usesCleartextTraffic="true": Permite HTTP para desarrollo
    - En producción, cambiar a false y usar solo HTTPS
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ============================================ -->
    <!-- PERMISOS DEL SISTEMA                         -->
    <!-- ============================================ -->
    
    <!-- Acceso a Internet (necesario para API Groq y GitHub) -->
    <uses-permission android:name="android.permission.INTERNET" />
    
    <!-- Verificar estado de la red (WiFi, datos móviles) -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- Acceso a almacenamiento externo para juegos HTML (solo Android < 10) -->
    <uses-permission 
        android:name="android.permission.READ_EXTERNAL_STORAGE" 
        android:maxSdkVersion="32" />
    
    <!-- Escritura en almacenamiento externo (solo Android < 10) -->
    <uses-permission 
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"
        tools:ignore="ScopedStorage" />
    
    <!-- Vibración para ejercicios de respiración -->
    <uses-permission android:name="android.permission.VIBRATE" />
    
    <!-- Evitar que el dispositivo se duerma durante ejercicios de respiración -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    
    <!-- Acceso a notificaciones (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    
    <!-- ============================================ -->
    <!-- CARACTERÍSTICAS DEL DISPOSITIVO              -->
    <!-- ============================================ -->
    
    <!-- La app requiere pantalla táctil (no funciona en Android TV/ Wear) -->
    <uses-feature 
        android:name="android.hardware.touchscreen"
        android:required="true" />
    
    <!-- La app funciona sin cámara (requerido para algunos juegos HTML) -->
    <uses-feature 
        android:name="android.hardware.camera"
        android:required="false" />

    <!-- ============================================ -->
    <!-- APLICACIÓN                                   -->
    <!-- ============================================ -->
    <application
        android:name=".SanaApplication"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sana"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="34">
        
        <!-- ============================================ -->
        <!-- ACTIVIDAD PRINCIPAL                         -->
        <!-- ============================================ -->
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Sana"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:screenOrientation="portrait">
            
            <!-- Intent filter para iniciar la app desde el launcher -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            
            <!-- Deep links para compartir contenido entre usuarios -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                
                <!-- Esquema personalizado: sana:// -->
                <data android:scheme="sana" />
            </intent-filter>
        </activity>
        
        <!-- ============================================ -->
        <!-- PROVEEDORES DE CONTENIDO                    -->
        <!-- ============================================ -->
        
        <!-- FileProvider para compartir archivos (guías, bitácoras) -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        
        <!-- ============================================ -->
        <!-- METADATA                                    -->
        <!-- ============================================ -->
        
        <!-- Soporte para pantalla completa en juegos -->
        <meta-data
            android:name="android.max_aspect"
            android:value="2.4" />
        
    </application>
</manifest>